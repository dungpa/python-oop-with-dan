# Define a ContactForm class which shows a GUI
# - The GUI should show labels and text fields for a contact form on a website, including:
#   = A name section
#   = An email section
# - Give the labels and text fields the same font family, but make the labels larger
# - Also define a start function in this class which shows the GUI
# Instantiate a ContactForm object and call its start() function to test
