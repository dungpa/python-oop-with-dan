# Define a SearchForm class which shows a GUI for the search section of a shopping website
# - Include labels/text fields for the following search fields
#   = Item Name
#   = Price (Lowest)
#   = Price (Highest)
#   = Release Date
# - Change the background colour of the window/labels so they use a dark blue
# - Change the text colour of the labels so they are white
# - Also define a start function in this class which shows the GUI
# Instantiate a SearchForm object and call its start() function to test
