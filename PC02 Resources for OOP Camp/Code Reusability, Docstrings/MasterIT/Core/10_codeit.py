# Define a function which accepts a list of integer values and a target integer value
# - This function should look through the list for that target integer
#   = If the target integer is found, the function should return the index of that target integer
#   = Otherwise, the function should return -1
# Call this function and output the returned index to test
