# Define a function which repeatedly asks the user to add words to a list
# - If they input an empty string, the function should stop asking and return that list
# - Add a docstring to this function which is fully descriptive
# Call this function and output the returned list to test
