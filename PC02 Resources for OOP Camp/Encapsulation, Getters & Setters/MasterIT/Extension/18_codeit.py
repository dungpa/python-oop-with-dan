# Define a 'Book' class
# - Give it a constructor with parameters for the Book's name, author, and genre (all strings)
#   = Declare protected instance variables for all three
# - Define public getter functions for all protected instance variables
# Define a 'Biography' child class of 'Book'
# - Override the constructor
#   = It should still accept the previous three parameters (name/author/genre)
#       == But add an additional parameter for 'ghost_author'
#   = Call the 'Book' class's constructor and pass in the name/author/genre parameters as arguments
#   = Declare a private instance variable for the ghost_author value
# - Define a getter function for the ghost_author private instance variable
#
# Instantiate one Book and one Biography object (using any values for the name/author/genre/ghost_author)
# - Output their information using their getter functions
