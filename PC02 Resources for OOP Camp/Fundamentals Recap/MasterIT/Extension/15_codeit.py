# Define the following string based functions:
#   - get_num_vowels: takes a string parameter and returns the number of vowels found in it
#   - get_num_consonants: takes a string parameter and returns the number of consonants found in it
#   - get_substring: takes two integer parameters and one string parameter, and returns a string between the letters at those integer parameters
#   - is_palindrome: takes a string parameter and returns True/False if it is a palindrome (same forwards as it is backwards)
