# Fix the errors in this program
# - It defines a class called 'Fridge'
#   = This class contains a constructor with two parameters and declares instance variables for both parameters
# - It then instantiates a Fridge object
class Fridge:
    def __int__(self, power: float, size: str):
        power = self.power
        self size = size


my_fridge = Fridge(250, "Large")
