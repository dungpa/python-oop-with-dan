# Fix the errors in this program
def to_bytes(bits: int) -> float:
    ""
    Converts bits into bytes

    param bits: the bits to convert
    return: the amount of bytes for the given bits
    ""
    return bits / 8


print(to_bytes(800))
