# Define a function which asks the user for three integers (start/end/step) and outputs that range
# - The function should validate the start/end/stop so they are all integers
# Call this function to test
