# Edit the 'output_height' function
# - Output the following messages based on the user's height
#   = If their height is less than 1, output "That is a short height"
#   = Otherwise, if their height is less than 1.2, output "Getting taller"
#   = Otherwise, if their height is less than 1.5, output "Tall indeed"
#   = Otherwise, output "Very tall!"
def output_height():
    height = float(input("What is your height (in metres): "))


output_height()
