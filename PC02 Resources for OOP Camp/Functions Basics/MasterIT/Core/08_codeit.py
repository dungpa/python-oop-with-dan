# Define a function called 'get_words'
# - It should create an empty list of words
# - It should then repeatedly ask the user to input a new word
#   = If their word is "quit" the loop ends
#   = Otherwise, the function appends their word to the list
# - It then outputs the list of words the user input
# Call this function to test
